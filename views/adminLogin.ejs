<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration</title>
    <link rel="icon" href="/images/smartphone.png" type="image/icon type">
    <link rel="stylesheet" href="registration.css">

</head>

<body>
    <div class="navigation">
        <img src="./images/log2.PNG" alt="logo" width="80px" height="55px">

        <a href="http://127.0.0.1:9000/contact">Contact us</a>

        <a href="http://127.0.0.1:9000/registration">Registration </a>

        <a href="http://127.0.0.1:9000/">Home </a>
        <a href="http://127.0.0.1:9000/"><span>Awesome Mobile company</span></a>

    </div>
    <h3>Enter the details</h3>
    <div id="userData">
        <form onsubmit=" return validate()" method="POST" action="/adminValidation">
            <label for="name">Name:</label>
            <input type="text" name="name1" id="name" placeholder="Username"><br>
            <label for="Password">Password:</label>
            <input type="password" name="password" placeholder="Password"><br>

            <input type="submit" value="submit">
        </form>
    </div>

    <footer>
        <p class="p1">Sunil Kushawaha </p>
        <p class="p2"><b>gmail:</b> sunilkushawaha2014@gmail.com</p>
        <p class="p3">Phone Number:8217219109</p>
    </footer>



    <script>
        //Validating the user name and phone number
        function validate() {

            var name, phone;
            name = document.getElementById('name').value;
            mobile = document.getElementById('phone').value;

            //validating the name of the user
            var check1;
            check1 = checkName(name);
            if (check1) {
                alert('Invalid first name. Please Enter Your First Name.');

                document.getElementById('name').value = "";
                document.getElementById('name').focus();
                return false;
            }
            //validating the phone number of the user
            var check2 = checkPhoneNum(mobile);
            if (check2) {
                alert('Invalid mobile number. Please enter 10 digit phone number.');

                document.getElementById('phone').value = "";
                document.getElementById('phone').focus();
                return false;
            }



            //name validation
            function checkName(name) {
                var check = /([A-Z][a-z]{10,} )([A-Z][a-z]{10,} )?([A-Z][a-z]{10,})/gi;
                if (check.test(name) || name === "") {
                    return true;
                }
            }


            function checkPhoneNum(mobNum) {
                var check = /\D/gi; //\D - checks for any non digit char
                if (check.test(mobNum) || mobNum.length !== 10) {
                    return true;
                }
            }

        }
    </script>

</body>

</html>